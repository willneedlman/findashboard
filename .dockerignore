**/__pycache__
**/*.pyc
**/*.pyo
.git
.gitignore
venv
.venv
frontend/node_modules
frontend/.vite
backend/.env
backend/.cache
.cache
*.log
nohup.out
gcloud auth login
graphify-out
ui-ux-pro-max-skill
*.md
!README.md
app.py

# Dev tooling / agent caches — never used by the Docker build (frontend/ and
# backend/ are the only paths COPYed). Excluding these cuts the build context
# from ~524 MB to well under 50 MB, so every deploy uploads far less.
.claude
.aider*
.serena
.codex
.agents
.obsidian
design-mockups
frontend/dist
**/node_modules
.pytest_cache
.ruff_cache
.mypy_cache
.DS_Store
.vscode
.idea

# data/ holds a ~100MB local full-build backup + runtime caches; only the bundled
# supplier DB is COPYed into the image, so keep just that file in the build context.
data/*
!data/veridion_nodes.db
!data/port_performance.db
!data/renthub_snapshot.db
!data/consumer_spend.db
